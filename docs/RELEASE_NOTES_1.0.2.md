## ÉTER PRISMA v1.0.2

Tiny release with a big heart.

### New
- Signature legend in the GUI and in the "how this works" overlay:
  *created with ♥ by Asastu*.

### Unchanged
- DSP bit-identical. Parameter IDs and plugin ID unchanged.
- pluginval strictness 10 SUCCESS.

### Install
Run `eter-prisma-1.0.2-win64-setup.exe` (VST3 + CLAP), or unzip
`eter-prisma-1.0.2-win64.zip` into your plugin folders manually.
